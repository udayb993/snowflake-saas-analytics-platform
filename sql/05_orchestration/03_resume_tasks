



-- Resume the task (tasks are created in suspended state by default)
ALTER TASK SAAS_ANALYTICS.ORCHESTRATION.TASK_LOAD_TENANT_ENGAGEMENT_METRICS RESUME;
-- Resume the task (tasks are created in suspended state by default)
ALTER TASK SAAS_ANALYTICS.ORCHESTRATION.TASK_LOAD_USER_ENGAGEMENT_SNAPSHOT RESUME;
-- Resume the task (tasks are created in suspended state by default)
ALTER TASK SAAS_ANALYTICS.ORCHESTRATION.TASK_LOAD_CONTENT_PERFORMANCE_METRICS RESUME;


-- Resume the task (tasks are created in suspended state by default)
ALTER TASK SAAS_ANALYTICS.ORCHESTRATION.TASK_TRANSFORM_BRONZE_TO_SILVER RESUME;
-- Resume the task (tasks are created in suspended state by default)
ALTER TASK SAAS_ANALYTICS.ORCHESTRATION.TASK_DAILY_LOAD_BRONZE_DATA RESUME;